#include "main.h"

/**
 * _abs - Entry point
 *
 * @n: character to be verify the absolue value
 *
 * Return: absolue value of n
 */

int _abs(int n)
{

	if (n >= 0)
	{
		return (n);
	}
	else
	{	return (-n);
	}

}
